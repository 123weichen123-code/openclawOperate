# 2026-03-22

- 用户明确仓库边界：`/home/admin/.openclaw/workspace` 根目录下与定时任务、Claw 自我成长/记录相关的修改，提交到 `openclawOperate.git` 对应分支。
- `historyboard/` 是独立新项目；其中的修改、提交、分支管理都归属 `historyboard.git`，不能和 workspace 根仓库混提。
- 以后涉及提交前，需要先判断改动属于哪个仓库，再分别在对应仓库执行 pull / commit / push。
